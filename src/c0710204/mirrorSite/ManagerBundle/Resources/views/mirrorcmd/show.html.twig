{% extends '::base.html.twig' %}

{% block body -%}
    <h1>mirrorcmd</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Mirrorid</th>
                <td>{{ entity.mirrorid }}</td>
            </tr>
            <tr>
                <th>Cmd</th>
                <td>{{ entity.cmd }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('mirrorcmd') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('mirrorcmd_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
